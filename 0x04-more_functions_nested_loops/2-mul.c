#include "main.h"

/**
 *  *  * mul - only function.
 *   *   * @a: - the input parameter
 *    *   * @b: - the input parameter
 *     *    * Return: Always an integer
 */
int mul(int a, int b)
{
	int nu;

	nu = a * b;
	return (nu);
}

